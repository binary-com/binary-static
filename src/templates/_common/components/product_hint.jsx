import React from 'react';

export const Asterisk = () => (
    <span className='product-hint'>*</span>
);

export const ProductHint = () => (
    <div className='hint' id='product_hint'>*{it.L('Some products are not available in all countries.')}</div>
);
