import React from 'react';

const Authenticate = () => (
    <React.Fragment>
        <h1>{it.L('Onfido Demo')}</h1>
        <div id='onfido' />
    </React.Fragment>
);

export default Authenticate;
