module.exports = {
    all: {
        // options: {
        //     syntax: require('postcss-scss') // work with SCSS directly
        // },
        src: ['src/sass/**/*.scss', '!src/sass/external/**/*.scss']
    }
};
